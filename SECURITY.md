# 安全说明（Security）

token-wallet 是**自托管**的个人多平台 AI 余额监控工具。请先理解下面的信任边界再部署。

## 凭证如何被保护

- 三家平台的凭证（DeepSeek/Kimi 模型 Key、火山 AK/SK）在服务端统一使用 **AES-256-GCM** 加密为一个密文 blob，每凭证随机 12 字节 IV，密文、IV 与 authTag 分别落库。
- 主密钥来自环境变量 `ENCRYPTION_KEY`（32 字节，64 位 hex）。**该密钥与数据库分离**；丢失该密钥，已存库的凭证密文将永久无法解密。
- 明文凭证只存在于「向对应平台发起余额请求的那一瞬间」的内存中，绝不进入日志、错误追踪、API 响应或邮件。
- 对外仅展示脱敏标识：Bearer 显示末 4 位；火山显示 AK 前缀 + 尾 4 位，SecretAccessKey 永不展示。

## 信任边界（重要）

**这是自托管应用，服务端在功能上必须能够解密你的凭证**（否则无法替你调用平台余额接口）。因此：

1. 只在你**信任的运行环境**部署（你自己的 Vercel / 服务器 / 数据库）。
2. 妥善保管 `ENCRYPTION_KEY`，不要提交到 git，不要与数据库放在同一处泄露面。
3. 项目开源不代表平台能"凭空"解密你的凭证——解密依赖你的 `ENCRYPTION_KEY` 与数据库密文，两者都泄露才构成泄露。

## 火山引擎（豆包）最小权限

查余额走费用中心 `QueryBalanceAcct`，属于账号级能力。**请务必**：

- 为 token-wallet 创建**独立的 IAM 子用户**，只绑定费用中心只读策略（推荐系统预设 `BillingCenterReadOnlyAccess`，或自定义 `billing:QueryBalanceAcct` 最小权限）；
- **不要使用主账号 AK/SK**，也不要使用 ARK 推理 Key；
- 录入页与接口层会拒绝非 `AKLT` 开头的 AK，并对「无 billing 权限」返回 `FORBIDDEN_SCOPE` 提示，但主账号 AK 在格式上与子用户无法静态区分，因此**最小权限依赖你正确配置 IAM**。

这样即使凭证泄露，攻击面也被限制为「只读你的账单」，无法开资源、调模型或改动资产。

## 报告安全问题

如发现安全相关问题，请通过 GitHub Issues 提交（避免公开泄露细节时，可先邮件联系维护者）。
